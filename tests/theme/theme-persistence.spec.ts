/**
 * Theme persistence tests have been split into focused files:
 * - theme-persistence-refresh.spec.ts  (page refresh and navigation tests)
 * - theme-persistence-auth.spec.ts     (login/logout and tenant isolation tests)
 */
