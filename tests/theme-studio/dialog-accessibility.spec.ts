/**
 * Dialog accessibility tests have been split into focused files:
 * - dialog-accessibility-hook.spec.ts    (useDialog hook dialogs: Integration, Plugin)
 * - dialog-accessibility-custom.spec.ts  (custom overlay dialogs: Role, Task, Order, etc.)
 */
