/**
 * Dark mode tests have been split into focused files:
 * - dark-mode-flash.spec.ts   (no white flash on load tests)
 * - dark-mode-theme.spec.ts   (theme toggle and status badge color tests)
 */
